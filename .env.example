# HuggingFace token — used for downloading the base model only
HF_TOKEN=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

# Base model ID to download and fine-tune (never exposed in outputs)
# Set this to your HuggingFace model ID, then keep .env private
BASE_MODEL_ID=google/medgemma-27b-it

# Tokenizer chat template identifier (required for fine-tuning — set in private .env)
CHAT_TEMPLATE=

# Optional: WandB monitoring (run will be named rosemed-27b-bg)
WANDB_API_KEY=
